# Virtual Machine deploy

## Prerequiste
1. vSphere vDatacenter prepared with Terraform (directory datacenter)
2. Template (directory template/packer)

## Deploy
1. Run `terraform init`
2. Run `terraform plan` and `terraform apply`